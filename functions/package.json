{
  "name": "trades-functions",
  "description": "Cloud Functions for Trades app – profile image upload (avoids RN Blob issue)",
  "main": "index.js",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  }
}
